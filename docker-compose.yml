version: '3'

services:
  springboot-docker-compose-app-container:
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - "80:5000"
    depends_on:
    - friendservice
    volumes:
    - .:/code
    networks:
    - mynet
  friendservice:
    build: ./Pirmas
    command: python app.py
    ports:
    - 5000:5001
    networks:
    - mynet
    
networks:
    mynet: